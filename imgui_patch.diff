diff --git a/backends/imgui_impl_opengl3.cpp b/backends/imgui_impl_opengl3.cpp
index ff99800..5dfb2da 100644
--- a/backends/imgui_impl_opengl3.cpp
+++ b/backends/imgui_impl_opengl3.cpp
@@ -248,0 +249 @@ struct ImGui_ImplOpenGL3_Data
+    GLint           AttribLocationTexChannels;
@@ -385,0 +387 @@ static void ImGui_ImplOpenGL3_SetupRenderState(ImDrawData* draw_data, ImGui_Impl
+    glUniform1i(bd->AttribLocationTexChannels, 0);
@@ -591,2 +593,9 @@ void    ImGui_ImplOpenGL3_RenderDrawData(ImDrawData* draw_data)
-                // Bind texture, Draw
-                GL_CALL(glBindTexture(GL_TEXTURE_2D, (GLuint)(intptr_t)pcmd->GetTexID()));
+                // Bind texture, select the TRex channel conversion, Draw
+                ImTextureID tex_id = pcmd->GetTexID();
+#if defined(__EMSCRIPTEN__)
+                const unsigned int texture_channels = 0;
+#else
+                const unsigned int texture_channels = IMGUI_TREX_OPENGL_TEXTURE_CHANNELS(tex_id);
+#endif
+                GL_CALL(glUniform1i(bd->AttribLocationTexChannels, (GLint)texture_channels));
+                GL_CALL(glBindTexture(GL_TEXTURE_2D, (GLuint)IMGUI_TREX_OPENGL_TEXTURE_NAME(tex_id)));
@@ -878,0 +888 @@ bool    ImGui_ImplOpenGL3_CreateDeviceObjects()
+        "uniform int TextureChannels;\n"
@@ -883 +893,6 @@ bool    ImGui_ImplOpenGL3_CreateDeviceObjects()
-        "    gl_FragColor = Frag_Color * texture2D(Texture, Frag_UV.st);\n"
+        "    vec4 Tex_Color = texture2D(Texture, Frag_UV.st);\n"
+        "    if (TextureChannels == 1)\n"
+        "        Tex_Color = vec4(Tex_Color.rrr, 1.0);\n"
+        "    else if (TextureChannels == 2)\n"
+        "        Tex_Color = Tex_Color.rrrg;\n"
+        "    gl_FragColor = Frag_Color * Tex_Color;\n"
@@ -887,0 +903 @@ bool    ImGui_ImplOpenGL3_CreateDeviceObjects()
+        "uniform int TextureChannels;\n"
@@ -893 +909,6 @@ bool    ImGui_ImplOpenGL3_CreateDeviceObjects()
-        "    Out_Color = Frag_Color * texture(Texture, Frag_UV.st);\n"
+        "    vec4 Tex_Color = texture(Texture, Frag_UV.st);\n"
+        "    if (TextureChannels == 1)\n"
+        "        Tex_Color = vec4(Tex_Color.rrr, 1.0);\n"
+        "    else if (TextureChannels == 2)\n"
+        "        Tex_Color = Tex_Color.rrrg;\n"
+        "    Out_Color = Frag_Color * Tex_Color;\n"
@@ -898,0 +920 @@ bool    ImGui_ImplOpenGL3_CreateDeviceObjects()
+        "uniform int TextureChannels;\n"
@@ -904 +926,6 @@ bool    ImGui_ImplOpenGL3_CreateDeviceObjects()
-        "    Out_Color = Frag_Color * texture(Texture, Frag_UV.st);\n"
+        "    vec4 Tex_Color = texture(Texture, Frag_UV.st);\n"
+        "    if (TextureChannels == 1)\n"
+        "        Tex_Color = vec4(Tex_Color.rrr, 1.0);\n"
+        "    else if (TextureChannels == 2)\n"
+        "        Tex_Color = Tex_Color.rrrg;\n"
+        "    Out_Color = Frag_Color * Tex_Color;\n"
@@ -910,0 +938 @@ bool    ImGui_ImplOpenGL3_CreateDeviceObjects()
+        "uniform int TextureChannels;\n"
@@ -914 +942,6 @@ bool    ImGui_ImplOpenGL3_CreateDeviceObjects()
-        "    Out_Color = Frag_Color * texture(Texture, Frag_UV.st);\n"
+        "    vec4 Tex_Color = texture(Texture, Frag_UV.st);\n"
+        "    if (TextureChannels == 1)\n"
+        "        Tex_Color = vec4(Tex_Color.rrr, 1.0);\n"
+        "    else if (TextureChannels == 2)\n"
+        "        Tex_Color = Tex_Color.rrrg;\n"
+        "    Out_Color = Frag_Color * Tex_Color;\n"
@@ -971,0 +1005 @@ bool    ImGui_ImplOpenGL3_CreateDeviceObjects()
+    bd->AttribLocationTexChannels = glGetUniformLocation(bd->ShaderHandle, "TextureChannels");
diff --git a/imconfig.h b/imconfig.h
index b40db38..8cc70fd 100644
--- a/imconfig.h
+++ b/imconfig.h
@@ -16,0 +17,7 @@
+// TRex stores the OpenGL texture name in the low 32 bits and the source
+// channel count in the high byte. Other renderers keep using the unmodified
+// ImTextureID value.
+#define IMGUI_TREX_OPENGL_TEXTURE_ID(_NAME, _CHANNELS) ((ImTextureID)((unsigned long long)(_NAME) | ((unsigned long long)(_CHANNELS) << 56)))
+#define IMGUI_TREX_OPENGL_TEXTURE_NAME(_ID) ((unsigned int)((unsigned long long)(_ID) & 0xFFFFFFFFULL))
+#define IMGUI_TREX_OPENGL_TEXTURE_CHANNELS(_ID) ((unsigned int)(((unsigned long long)(_ID) >> 56) & 0xFFULL))
+
diff --git a/imgui.cpp b/imgui.cpp
--- a/imgui.cpp
+++ b/imgui.cpp
@@ -5179 +5179 @@ static void SetupDrawListSharedData()
-    g.DrawListSharedData.InitialFlags = ImDrawListFlags_None;
+    g.DrawListSharedData.InitialFlags = ImDrawListFlags_TextNoPixelSnap;
